---

---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="viewport" content="width=device-width" />
		<title>Agent Quota</title>
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500;600&family=Instrument+Sans:wght@400;500;600;700&display=swap" rel="stylesheet" />
		<style>
			:root {
				--bg-primary: #0a0a0b;
				--bg-secondary: #111113;
				--bg-tertiary: #18181b;
				--border-dim: rgba(255,255,255,0.06);
				--border-subtle: rgba(255,255,255,0.1);
				--text-primary: #fafafa;
				--text-secondary: #a1a1aa;
				--text-tertiary: #52525b;
				--accent-green: #22c55e;
				--accent-amber: #f59e0b;
				--accent-cyan: #06b6d4;
				--accent-violet: #8b5cf6;
			}

			* { margin: 0; padding: 0; box-sizing: border-box; }

			body {
				background: var(--bg-primary);
				color: var(--text-primary);
				font-family: 'Instrument Sans', system-ui, sans-serif;
				min-height: 100vh;
				-webkit-font-smoothing: antialiased;
				overflow-x: hidden;
			}

			.noise {
				position: fixed;
				inset: 0;
				pointer-events: none;
				opacity: 0.03;
				background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E");
				z-index: 99;
			}

			.grid-bg {
				position: fixed;
				inset: 0;
				pointer-events: none;
				z-index: 0;
				background-image:
					linear-gradient(rgba(255,255,255,0.015) 1px, transparent 1px),
					linear-gradient(90deg, rgba(255,255,255,0.015) 1px, transparent 1px);
				background-size: 80px 80px;
			}

			.page {
				position: relative;
				z-index: 1;
				min-height: 100vh;
				display: grid;
				grid-template-rows: auto 1fr auto;
			}

			/* Nav */
			nav {
				display: flex;
				justify-content: flex-end;
				align-items: center;
				padding: 1.5rem 3rem;
				animation: fadeIn 0.5s ease-out both;
			}

			.nav-label {
				font-family: 'IBM Plex Mono', monospace;
				font-size: 0.6rem;
				letter-spacing: 0.15em;
				text-transform: uppercase;
				color: var(--text-tertiary);
			}

			/* Content */
			.content {
				display: grid;
				grid-template-columns: 1fr 1.4fr;
				gap: 4rem;
				padding: 0 3rem;
				align-items: center;
				animation: fadeIn 0.6s ease-out 0.1s both;
			}

			.text-col {
				padding-left: 4vw;
			}

			.overline {
				font-family: 'IBM Plex Mono', monospace;
				font-size: 0.6rem;
				letter-spacing: 0.2em;
				text-transform: uppercase;
				color: var(--accent-cyan);
				opacity: 0.7;
				margin-bottom: 1rem;
				display: flex;
				align-items: center;
				gap: 0.6rem;
			}

			.overline::before {
				content: '';
				width: 20px;
				height: 1px;
				background: var(--accent-cyan);
				opacity: 0.4;
			}

			h1 {
				font-size: clamp(2.2rem, 4.5vw, 3.5rem);
				font-weight: 700;
				letter-spacing: -0.04em;
				line-height: 1.05;
				margin-bottom: 1.5rem;
			}

			.desc {
				color: var(--text-secondary);
				font-size: 0.95rem;
				line-height: 1.7;
				max-width: 380px;
				margin-bottom: 2.5rem;
			}

			/* Services list (from design2) */
			.services-list {
				display: flex;
				flex-direction: column;
				gap: 0.6rem;
				margin-bottom: 3rem;
			}

			.svc {
				display: flex;
				align-items: center;
				gap: 0.6rem;
				font-family: 'IBM Plex Mono', monospace;
				font-size: 0.75rem;
				color: var(--text-tertiary);
			}

			.svc-dot {
				width: 6px;
				height: 6px;
				border-radius: 50%;
			}

			.svc-name {
				color: var(--text-secondary);
			}

			/* Actions (from design2) */
			.actions {
				display: flex;
				gap: 1rem;
				align-items: center;
			}

			.gh-btn {
				display: inline-flex;
				align-items: center;
				gap: 0.5rem;
				background: var(--text-primary);
				color: var(--bg-primary);
				text-decoration: none;
				font-family: 'IBM Plex Mono', monospace;
				font-size: 0.78rem;
				font-weight: 600;
				padding: 0.65rem 1.4rem;
				border-radius: 6px;
				transition: all 0.2s;
			}
			.gh-btn:hover {
				background: var(--accent-cyan);
				transform: translateY(-1px);
			}
			.gh-btn svg { width: 15px; height: 15px; fill: currentColor; }

			.local-badge {
				font-family: 'IBM Plex Mono', monospace;
				font-size: 0.6rem;
				color: var(--accent-green);
				letter-spacing: 0.1em;
				text-transform: uppercase;
				opacity: 0.7;
			}

			/* Screenshots — overlapping (design5 style), noctalia on top */
			.screenshots-col {
				position: relative;
				padding: 2rem 0;
			}

			.ss-web {
				width: 95%;
				border-radius: 10px;
				border: 1px solid var(--border-dim);
				display: block;
				position: relative;
				z-index: 3;
				margin-top: -20%;
				margin-left: 10%;
				transition: transform 0.4s, box-shadow 0.4s;
			}
			.ss-web:hover {
				transform: translateY(-4px);
				box-shadow: 0 16px 60px rgba(0,0,0,0.5);
			}

			.ss-noctalia {
				width: 55%;
				border-radius: 10px;
				border: 1px solid var(--border-dim);
				display: block;
				position: relative;
				z-index: 2;
				margin-left: -2%;
				margin-right: auto;
				transition: transform 0.4s, box-shadow 0.4s;
			}
			.ss-noctalia:hover {
				transform: translateY(-4px);
				box-shadow: 0 16px 60px rgba(0,0,0,0.5);
			}

			.ss-label {
				font-family: 'IBM Plex Mono', monospace;
				font-size: 0.6rem;
				letter-spacing: 0.1em;
				text-transform: uppercase;
				color: var(--text-tertiary);
				margin-top: 0.6rem;
			}

			.ss-label-web { text-align: left; padding-left: 0.5rem; }
			.ss-label-noc { text-align: right; padding-right: 2%; }

			/* Footer */
			.foot {
				padding: 2rem 3rem;
				display: flex;
				justify-content: space-between;
				align-items: center;
				animation: fadeIn 0.6s ease-out 0.2s both;
			}

			.foot-text {
				font-family: 'IBM Plex Mono', monospace;
				font-size: 0.6rem;
				color: var(--text-tertiary);
				letter-spacing: 0.05em;
			}

			.foot-line {
				flex: 1;
				height: 1px;
				background: linear-gradient(to right, var(--border-dim), transparent);
				margin: 0 2rem;
			}

			@keyframes fadeIn {
				from { opacity: 0; transform: translateY(10px); }
				to { opacity: 1; transform: translateY(0); }
			}

			@media (max-width: 800px) {
				.content {
					grid-template-columns: 1fr;
					padding: 2rem 1.5rem;
					gap: 2rem;
				}
				.text-col { padding-left: 0; }
				nav, .foot { padding: 1.5rem; }
				.ss-noctalia {
					margin-top: -10%;
					width: 60%;
				}
			}
		</style>
	</head>
	<body>
		<div class="noise"></div>
		<div class="grid-bg"></div>

		<div class="page">
			<nav>
				<span class="nav-label">Open Source</span>
			</nav>

			<div class="content">
				<div class="text-col">
					<div class="overline">Local Dashboard</div>
					<h1>Agent<br/>Quota</h1>
					<p class="desc">
						Monitor your AI coding assistant usage quotas. Track rate limits and credit balances across all your subscriptions from one place.
					</p>

					<div class="services-list">
						<div class="svc">
							<span class="svc-dot" style="background: var(--accent-green)"></span>
							<span class="svc-name">Claude</span>
							<span>&mdash; usage tracking</span>
						</div>
						<div class="svc">
							<span class="svc-dot" style="background: var(--accent-amber)"></span>
							<span class="svc-name">Codex</span>
							<span>&mdash; usage tracking</span>
						</div>
						<div class="svc">
							<span class="svc-dot" style="background: var(--accent-violet)"></span>
							<span class="svc-name">Zai</span>
							<span>&mdash; usage tracking</span>
						</div>
						<div class="svc">
							<span class="svc-dot" style="background: var(--accent-cyan)"></span>
							<span class="svc-name">OpenRouter</span>
							<span>&mdash; credits & balance</span>
						</div>
						<div class="svc">
							<span class="svc-dot" style="background: #f472b6"></span>
							<span class="svc-name">Opencode Zen</span>
							<span>&mdash; credits & balance</span>
						</div>
					</div>

					<div class="actions">
						<a href="https://github.com/peculiarnewbie/clanker-quota" target="_blank" rel="noopener" class="gh-btn">
							<svg viewBox="0 0 16 16"><path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0016 8c0-4.42-3.58-8-8-8z"/></svg>
							GitHub
						</a>
						<span class="local-badge">100% local</span>
					</div>
				</div>

				<div class="screenshots-col">
					<img class="ss-noctalia" src="/screenshot-noctalia.png" alt="Agent Quota Noctalia plugin" />
					<div class="ss-label ss-label-noc">Noctalia Plugin</div>
					<img class="ss-web" src="/screenshot-vite.png" alt="Agent Quota web dashboard" />
					<div class="ss-label ss-label-web">Web Dashboard</div>
				</div>
			</div>

			<div class="foot">
				<span class="foot-text">Runs entirely on localhost</span>
				<div class="foot-line"></div>
				<span class="foot-text">No data leaves your machine</span>
			</div>
		</div>
	</body>
</html>
